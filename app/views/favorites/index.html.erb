<div class="row">
    <div class="col-xs-10 col-xs-offset-1">
      <h1>Favorites</h1>
      <% @user.favorite_users.each do |f| %>

        <p><%= f.username %></p>

      <% end %>
    </div>
</div>